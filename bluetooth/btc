#!/bin/sh
#
# connects an previously paired bluetooth device

# list names of paired devices
btls pn | dmenu | sort -u | while read -r name
do
	# convert name to id
	id=$(btconvert -i "$name")
	# disconnect using id
	[ -n "$id" ] && echo "connect $id" | bluetoothctl
done

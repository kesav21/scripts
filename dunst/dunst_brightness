#!/bin/bash


source ~/.bin/dunst_ids

icon=~/.icons/fa-5.10.1-desktop/svgs/solid/adjust.svg
brightness=$(~/.bin/bright_get | grep -o '[0-9]' | paste -sd '')
bar=$(~/.bin/dunst_bar $brightness 100)

dunstify -i $icon -t 2000 -r $id_brightness 'Brightness' "\n$bar"


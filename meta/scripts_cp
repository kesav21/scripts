#!/bin/sh

cd ~/.scripts/
files=$(find . \( ! -regex '.*/\..*' \) -type f)

echo "\nclearing ~/.bin:\n"

rm -rfv ~/.bin/ && mkdir ~/.bin/

echo "\nadding scripts to ~/.bin/\n"

for file in $files
do
	if [ -x "$file" ]
	then
		ln -sv $(realpath $file) ~/.bin/$(basename $file)
	fi
done

echo "\n"


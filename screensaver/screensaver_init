#!/bin/bash

# retrieve shared variables
source screensaver_vars

# initialize dpms
md=$(dpms_init $standby_timeout $suspend_timeout $off_timeout)

# initialize xautolock
mx=$(xautolock_init $locker_timeout)

# send notification
dunst_screensaver "$md" "$mx"


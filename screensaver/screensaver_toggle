#!/bin/bash

# retrieve shared variables
source ~/.bin/screensaver_vars

# toggle dpms
md=$(~/.bin/dpms_toggle)

# toggle xautolock
mx=$(~/.bin/xautolock_toggle $locker_timeout)

# send notification
~/.bin/dunst_screensaver $md $mx

